# 修改日志

## 2026-05-25 09:00:47 CST

- 分析项目中普通测速、预检、稳定模式复测、反代 IP 优选、Worker 真实出口验证和 Clash path 生成逻辑。
- 新增 `测速与反代测试逻辑分析.md`，整理测速逻辑和反代测试实现说明。
- 新增本文件，用于记录本次操作。

## 2026-05-25 14:06:24 CST

- 移除本地 Clash 配置生成后端接口、`internal/clash` 生成模块和旧规则文件。
- 精简配置模型，只保留仍在使用的 `clash.proxyip_auto` 反代 IP 优选配置。
- 重构 Web UI 为高密度小工具布局：左侧菜单，右侧 `IP 优选`、`反代 IP`、`系统配置` 三个功能页。
- 新增 ADD/PROXYIP 结果快速复制、同步到 CF、系统配置可视化保存能力。
- 接入系统托盘：启动后可从托盘打开工具或退出。
- 新增 mac/Linux 本机打包脚本 `scripts/build.sh` 和 Windows 本机打包脚本 `scripts/build.ps1`。
- 更新 README 和测速逻辑分析文档，移除本地 Clash 生成相关旧说明。
- 执行 `go test ./...`、`go build -o dist/bestsub-tool ./cmd/bestsub` 和 `./scripts/build.sh` 验证通过。

## 2026-05-25 14:30:47 CST

- 引入 `github.com/webview/webview_go`，将默认启动方式改为内嵌系统 WebView 桌面窗口，不再自动打开外部浏览器。
- 保留系统托盘入口，托盘菜单可再次打开桌面窗口或退出工具。
- 新增 `-web` 参数，仅用于启动 HTTP 服务调试；默认 `-serve`/双击运行走桌面 GUI。
- 更新 README，说明 Windows/macOS 桌面 GUI 兼容方式和 Windows WebView2 Runtime 要求。
- 执行 `go test ./...` 和 `go build -o dist/bestsub-tool ./cmd/bestsub` 验证通过。

## 2026-05-25 15:11:18 CST

- 将桌面入口从临时 `systray + webview_go` 方案切换为 Wails 路线，默认启动 Wails 桌面窗口。
- 新增 `wails.json`，为后续使用 `wails build` 发版做准备。
- 保留 `-web` 调试模式，用于仅启动本地 HTTP 服务。
- 修正配置相对路径解析：当配置文件位于 `configs/` 时，`ADD.txt`、`GeoLite2-Country.mmdb`、`seeds.txt` 等路径按项目根目录解析。
- 更新 `scripts/build.sh` / `scripts/build.ps1`：检测到 `wails` CLI 时优先走 `wails build`。
- 更新 README，改为 Wails 桌面 GUI 语义说明。
- 执行 `go mod tidy`、`go test ./...`、`go build -o dist/bestsub-tool ./cmd/bestsub` 及 `-web` 模式接口检查通过。

## 2026-05-25 15:39:13 CST

- 将启动逻辑抽到 `internal/desktop/runner.go`，新增根目录 `main.go`，让 Wails 能识别项目根入口。
- 保留 `cmd/bestsub/main.go` 作为兼容入口，内部复用同一套桌面启动逻辑。
- 新增最小 `frontend/package.json` 和补充 `wails.json` 的 `frontend:dir`，避免 `wails build` 因缺少根入口或 frontend 目录报错。
- 执行 `gofmt`、`go test ./...`、`go build -o dist/bestsub-tool ./cmd/bestsub` 验证通过。
- 当前执行环境中 `wails` CLI 不在 PATH，无法代跑 `wails build`，但代码结构已调整为 Wails 标准可识别形式。

## 2026-05-25 18:15:47 CST

- 定位 `.app` 无法打开的原因：应用包内不存在 `configs/config.yaml`，Finder 启动时默认配置路径落在 `.app/Contents/MacOS/configs/`，导致运行行为异常。
- 将默认配置路径改为用户配置目录：
  macOS 为 `~/Library/Application Support/BestSub/config.yaml`。
- 增加首次运行自动初始化逻辑：若默认配置不存在，则自动创建 `config.yaml` 和 `seeds.txt`。
- 更新 README，说明桌面版默认配置位置与自动初始化行为。
- 通过删除用户配置目录后执行 `./dist/bestsub-tool -web` 验证：可自动创建默认配置并正常启动。
